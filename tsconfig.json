{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".",
    "paths": {
      "@Components": ["components"],
      "@Stores": ["stores"],
      "@Types": ["types"],
      "@Interfaces": ["interfaces"],
      "@Utils": ["utils"],
      "@Scripts": ["scripts"]
    }
  }
}
